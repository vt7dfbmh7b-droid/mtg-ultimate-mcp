{
  "name": "mtg-ultimate-mcp",
  "version": "0.12.0",
  "private": true,
  "type": "module",
  "description": "MCP-powered Magic: The Gathering knowledge, Commander rules, self-updating precon intelligence, themed printing-aware deck building, competing-package refinement, source diagnostics, deduplicated tournament evidence, simulation, pricing and combo analysis.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "tsx --test src/**/*.test.ts",
    "test:live": "tsx scripts/live-smoke.ts",
    "test:e2e": "tsx scripts/e2e-commander-scenario.ts",
    "check": "npm run build && npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/node": "2.0.0",
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.20.0",
    "typescript": "^7.0.2"
  }
}
