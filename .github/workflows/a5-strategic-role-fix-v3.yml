name: A5 Strategic Role Fix V3

on:
  push:
    branches:
      - test/counter-blitz-a5-semantic-frontier-20260829
    paths:
      - scripts/patch-a5-ranger-role-v3.mjs
      - scripts/e2e-a5-strategic-roles-v3.ts
      - .github/workflows/a5-strategic-role-fix-v3.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch-and-test-v3:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Check out isolated branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 1
      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version-file: .node-version
          package-manager-cache: false
      - name: Install dependencies
        run: npm ci
      - name: Apply Ranger-Captain semantic correction
        run: node scripts/patch-a5-ranger-role-v3.mjs
      - name: Build
        run: npm run build
      - name: Run strict strategic role regressions
        run: |
          set -o pipefail
          npx tsx scripts/e2e-a5-strategic-roles-v3.ts | tee a5-strategic-roles-v3.log
      - name: Commit tested Ranger-Captain correction
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/services/scryfall.ts
          if git diff --cached --quiet; then
            echo "No source changes to commit."
          else
            git commit -m "fix: classify Ranger-Captain as combo protection in A5"
            git push origin HEAD:test/counter-blitz-a5-semantic-frontier-20260829
          fi
      - name: Upload semantic evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: a5-strategic-roles-v3-${{ github.sha }}
          path: a5-strategic-roles-v3.log
          if-no-files-found: ignore
          retention-days: 14
