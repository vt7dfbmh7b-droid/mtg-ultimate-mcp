name: Counter Blitz A19 Iterative Slot Pressure

on:
  push:
    branches:
      - test/counter-blitz-a19-iterative-slot-pressure-20260829
  workflow_dispatch:

jobs:
  validate-a19:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Install dependencies
        run: npm ci
      - name: Build
        run: npm run build
      - name: Run full test suite
        run: npm test
      - name: Run A19 iterative slot pressure
        run: npx tsx scripts/e2e-counter-blitz-a19-iterative-slot-pressure.ts
      - name: Upload A19 evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: counter-blitz-a19-evidence-${{ github.sha }}
          path: |
            counter-blitz-a19-result.json
            counter-blitz-a19-leader.txt
          if-no-files-found: warn
