name: Scions A11 External Comparison v3

on:
  push:
    branches:
      - test/scions-compare-user-list-20260831
  workflow_dispatch:

jobs:
  compare-v3:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Compare A11 vs external Y'shtola build v3
        run: npx tsx scripts/e2e-scions-external-comparison-v3.ts
      - name: Upload comparison evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scions-external-comparison-v3-${{ github.sha }}
          retention-days: 90
          if-no-files-found: warn
          path: |
            scions-external-comparison-v3.json
            scions-external-comparison-v3.md
